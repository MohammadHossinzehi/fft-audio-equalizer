{
  "name": "fft-audio-equalizer",
  "version": "1.0.0",
  "description": "From-scratch FFT spectrum analyzer and biquad parametric equalizer for the browser.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
